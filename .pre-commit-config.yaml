#repos:
#  - repo: local
#    hooks:
#      - id: flake8
#        name: flake8
#        entry: poetry run flake8
#        language: system
#        types: [ python ]
#        exclude: '(\.venv|venv|__pycache__|\.git|\.mypy_cache|\.tox)/'
#        args:
#          - --ignore=E203,E266,W503,F821,F811,ANN002,ANN003,ANN101,ANN102,ANN401,N807,N818,VNE001,F401,W504
#          - --max-line-length=119
#          - --max-complexity=18
#          - --select=B,C,E,F,W,T4,B9,ANN,Q0,N8,VNE
#          - --exclude=.venv
#          - --extend-exclude=src/tests/*,src/database/migrations/*
# Quick Start:
#
# pip install pre-commit
# pre-commit install && pre-commit install -t pre-push
# pre-commit run --all-files
#
# To Skip Checks:
#
# git commit --no-verify
fail_fast: false


repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # ruff version.
    rev: v0.7.0
    hooks:
      # Run the linter.
      #
      # When running with --fix, Ruff's lint hook should be placed before Ruff's formatter hook,
      # and before Black, isort, and other formatting tools, as Ruff's fix behavior can output code changes that require reformatting.
      - id: ruff
        args: [--fix]
      # Run the formatter.
      - id: ruff-format
